/*
  Warnings:

  - You are about to drop the column `currentYear` on the `pagamento` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `pagamento` DROP COLUMN `currentYear`;
